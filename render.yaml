services:
  - type: web
    runtime: node
    name: eudp-country-of-origin
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
